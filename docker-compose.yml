services:
  terminaldeck:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
      # Mount your project directory to /workspace for terminal access
      - .:/workspace
    environment:
      - TERMINALDECK_PORT=3000
