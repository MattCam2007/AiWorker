version: '3.8'
services:
  terminaldeck:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
      - ./workspace:/workspace
    environment:
      - TERMINALDECK_PORT=3000
    restart: unless-stopped
